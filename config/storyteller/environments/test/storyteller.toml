# =============================================================================
# Storyteller — Test Environment Overrides
# =============================================================================
# Merged on top of base/storyteller.toml when STORYTELLER_ENV=test.

[database]
url = "${DATABASE_URL:-postgres://storyteller:storyteller@localhost:5435/storyteller_test}"

[database.pool]
max_connections = 5
min_connections = 1

[inference]
thread_pool_size = 2
